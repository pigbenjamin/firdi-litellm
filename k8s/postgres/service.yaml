apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: ai-platform
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  # 只給叢集內部（litellm 的 store_model_in_db）連線用，不對外曝露。
  type: ClusterIP
