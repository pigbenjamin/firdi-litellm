apiVersion: v1
kind: Service
metadata:
  name: fast-vllm-service
  namespace: ai-platform
  labels:
    app: fast-vllm
spec:
  selector:
    app: fast-vllm
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
